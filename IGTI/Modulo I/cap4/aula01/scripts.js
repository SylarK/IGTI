'use strict';

/*
  VARIÁVEIS
  var -> possui escopo amplo / abrangente
  let -> possui escopo reduzido
  const -> possui valor imutável (não se pode retribuir valores)

*/

/*
  FUNCTIONS

  - Normal
  function sum(a, b){
    return a + b;
  }
 
  - Anônima
  const sum2 = function(a,b){
    return a + b;
  }

  - Arrow Function
  const sum3 = (a,b) => {
    return a + b;
  }

  const sum4 = (a,b) => a + b;

*/
